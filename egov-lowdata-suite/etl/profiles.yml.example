egov_lowdata:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: ${POSTGRES_USER}
      password: ${POSTGRES_PASSWORD}
      port: 5432
      dbname: ${POSTGRES_DB}
      schema: public