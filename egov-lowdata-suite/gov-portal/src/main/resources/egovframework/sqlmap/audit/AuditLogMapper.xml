<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.govportal.audit.mapper.AuditLogMapper">

    <insert id="insertLog">
        insert into audit_logs (id, action, detail, severity, actor_id, created_at)
        values (#{id}, #{action}, #{detail}, #{severity}, #{actorId}, #{createdAt})
    </insert>

    <select id="selectLogs" parameterType="map"
            resultType="egovframework.govportal.audit.model.AuditLogVO">
        select id,
               action,
               detail,
               severity,
               actor_id    as actorId,
               created_at  as createdAt
        from audit_logs
        <where>
            <if test="severity != null and severity != ''">
                and severity = #{severity}
            </if>
            <if test="keyword != null and keyword != ''">
                and (
                    lower(action) like concat('%', lower(#{keyword}), '%')
                    or lower(detail) like concat('%', lower(#{keyword}), '%')
                )
            </if>
        </where>
        order by created_at desc
        limit #{limit}
    </select>

</mapper>
