<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="egovframework.govportal.auth.service.AccountRecoveryMapper">

    <select id="findUsernameByNameAndEmail" resultType="string">
        SELECT username
        FROM users
        WHERE name = #{name}
          AND email = #{email}
        LIMIT 1
    </select>

    <select id="verifyUserByNameEmailUsername" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM users
            WHERE name = #{name}
              AND email = #{email}
              AND username = #{username}
        )
    </select>

    <update id="updatePassword">
        UPDATE users
        SET password = #{password}
        WHERE username = #{username}
    </update>

    <select id="findEmailByUsername" resultType="string">
        SELECT email
        FROM users
        WHERE username = #{username}
        LIMIT 1
    </select>

</mapper>
